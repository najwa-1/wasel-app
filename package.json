{
  "name": "wasel-signaling-server",
  "version": "1.0.0",
  "description": "WebSocket signaling server for the Wasel remote desktop platform. Relays SDP and ICE only; media flows peer-to-peer.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
